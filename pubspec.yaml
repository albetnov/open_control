name: open_control
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  command_it: ^9.5.1
  flutter:
    sdk: flutter
  flutter_it: ^2.1.2
  get_it: ^9.2.1
  go_router: ^17.3.0
  shared_preferences: ^2.5.5
  validasi: ^1.0.0-rc.5
  validasi_annotation: ^0.1.0-dev.5
  validasi_ui: ^0.1.0-dev.6
  watch_it: ^2.4.2
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  validasi_gen: ^0.1.0-dev.6

flutter:
  uses-material-design: true
